# SMTP Configuration
# Copy this file to .env and fill in your SMTP settings

# SMTP Server (e.g., smtp.gmail.com, smtp-mail.outlook.com)
SMTP_SERVER=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Use TLS (true for port 587, false for port 465)
SMTP_USE_TLS=true

# SMTP Username (your email address)
SMTP_USERNAME=your-email@gmail.com

# SMTP Password (for Gmail, use an App Password, not your regular password)
SMTP_PASSWORD=your-app-password-here

# Sender Email (usually same as SMTP_USERNAME)
SENDER_EMAIL=your-email@gmail.com
